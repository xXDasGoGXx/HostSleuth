# HostSleuth v0.8.0 — Publication and Verification

Status: published and independently verified on 2026-09-17. Production/recovery rollout remains a separate step.

## Source

Stable `v0.8.0` was published from exact accepted source:

`28b8d88ec018782353840dee8528d765c4056e89`

Release workflow run:

`35294912224` — success

The GitHub release/tag targets the same exact commit.

## Native release assets

Published:

- `hostsleuth-linux-amd64` — `sha256:1ec0b31b88816ab74c42af9ed1b6ce4337c91f8b793ce0357e934c0bbb5644a0`
- `hostsleuth-linux-arm64` — `sha256:1a1854160594cc8926e1f103b3071a02be713ad38773addfdee8830ee07185bb`
- `SHA256SUMS` — `sha256:250e487218e466392b3c5b2df02512580093f0453dde59aead88d41edf84cad8`

Independent OMV verification downloaded all three public assets and `sha256sum -c SHA256SUMS` passed for both native binaries.

The published amd64 binary reported:

`v0.8.0 (28b8d88ec018)`

The public M14 proxy CLI was independently exercised. Invalid public URL input reached the proxy command's validation path and failed closed as expected.

## Docker publication

Published tags:

- `mjmalleo/hostsleuth:0.8.0`
- `mjmalleo/hostsleuth:latest`

Anonymous registry verification confirmed both tags resolve to the same OCI index:

`sha256:152255f44e451995767cd40b64b99879feb0ed0ae28a4fddfb4a269a55fc11df`

Platform manifests:

- linux/amd64 — `sha256:faaa1b76199462b5d577d9170b93353625a281b994153f71c065a19012df114b`
- linux/arm64 — `sha256:9e9b1464f66b208839793b246b92267f30a360e27e0c60b64271bac531558e09`

## Included milestone

v0.8.0 contains M14 Reverse Proxy / Upstream Story + Admin Console v2.

M14 adds:

- explicit public HTTP/HTTPS URL plus explicit expected upstream URL;
- deterministic public DNS/route/TCP/TLS/HTTP stages;
- deterministic upstream DNS/route/TCP/TLS/HTTP stages;
- local listener/Docker publication context when the public endpoint is proven local;
- native upstream Host/SNI probe;
- public Host/SNI probe against the same explicit upstream when identities differ;
- first proven failure / warning / unknown precedence;
- HTTP 4xx warning and 5xx failure semantics;
- bounded same-host redirects and cross-host redirect stop;
- query-string redaction in returned URL/Location evidence;
- `hostsleuth proxy` CLI;
- `GET /api/proxy-story`;
- visual Proxy Path Web UI;
- Admin Console v2 Proxy Quick Target action and bounded evidence-copy controls.

## Rollout boundary

Publication is not production acceptance.

At this publication closeout:

- stable/public = v0.8.0;
- live Arcane production = v0.7.0;
- `OMV-Docker-Rebuild` recovery pin = v0.7.0.

The recovery 0.8.0 pin may be staged, but it must not merge before live production is upgraded and passes acceptance.

# HostSleuth v0.7.0 — Production / Recovery Alignment

Status: complete on 2026-09-17.

## Published source

Stable v0.7.0 was published from:

`236002106afd6aa042fd131c0edc0f3455b9cfdf`

Verified public OCI index:

`sha256:3663e8c483b67de72f3a0e26fd80e9e3686319d9b2bafe602979cd790e2ce2bb`

## Live Arcane redeploy

The existing Arcane-managed HostSleuth project was redeployed by changing only the image tag from:

`mjmalleo/hostsleuth:0.6.0`

to:

`mjmalleo/hostsleuth:0.7.0`

All other deployment settings were preserved.

## Live acceptance

Post-redeploy verification confirmed:

- `GET /api/about` reports `v0.7.0`;
- `GET /api/snapshot` reports schema version 4 and Docker mode;
- the live HostSleuth container record uses `mjmalleo/hostsleuth:0.7.0`;
- 100 recent retained events are still available;
- retained history includes events from before the v0.7.0 redeploy, proving state continuity;
- LAN access to `/api/actions` still fails closed with HTTP 403 and the loopback-only message;
- a loopback-source request to `/api/actions` still returns the `service.restart` capability as `enabled=false` and `available=false`;
- live DNS Detective compared the system resolver with one explicitly supplied local resolver and returned `agree`;
- live served JavaScript contains DNS Detective, Quick Target, density control, and the DNS API path;
- live served CSS contains the Admin Console rail and DNS resolver-card layout;
- headless-browser DOM execution confirmed the Quick Target deck, DNS navigation/view, density control, and recent-target container.

The temporary Cloudflare Arcane tunnel used before the user returned home was stopped cleanly after the redeploy; no temporary tunnel remains active.

## Disaster recovery

`xXDasGoGXx/OMV-Docker-Rebuild` recovery PR #7 updated the HostSleuth recovery pin to:

`mjmalleo/hostsleuth:0.7.0`

while preserving:

- host networking;
- host PID/UTS namespaces;
- the existing persistent state path;
- the LAN bind address;
- read-only Docker socket;
- read-only container filesystem;
- dropped Linux capabilities;
- `no-new-privileges`;
- existing tmpfs settings.

Recovery PR #7 merged at:

`80e7c7c08b5905c7fbad158e4b11e6e4b4ced1f9`

## Final alignment

Stable/public, live Arcane production, and disaster recovery are all aligned on v0.7.0.

The next approved feature milestone is M14 Reverse Proxy / Upstream Story, with Admin Console v2 UX work progressing alongside it.

# HostSleuth v0.8.0 — Production / Recovery Alignment

Status: complete on 2026-09-17 local time.

## Published release

Stable `v0.8.0` was published from exact accepted source:

`28b8d88ec018782353840dee8528d765c4056e89`

Release workflow:

`35294912224` — success

Verified public Docker OCI index:

`sha256:152255f44e451995767cd40b64b99879feb0ed0ae28a4fddfb4a269a55fc11df`

## Live Arcane rollout

The existing Arcane-managed HostSleuth project was redeployed in place from:

`mjmalleo/hostsleuth:0.7.0`

to:

`mjmalleo/hostsleuth:0.8.0`

No other Compose setting was intentionally changed.

## Live acceptance

Post-redeploy verification confirmed:

- `GET /api/about` reports `v0.8.0`;
- `GET /api/snapshot` reports schema 4 / Docker mode;
- the live HostSleuth container record uses `mjmalleo/hostsleuth:0.8.0`;
- 100 retained events remain available;
- retained history includes events from before the v0.8.0 redeploy, proving state continuity;
- LAN access to `/api/actions` still fails closed with HTTP 403 and the loopback-only message;
- a loopback-source request to `/api/actions` still reports `service.restart` as `enabled=false` / `available=false`.

## M14 Proxy Path live acceptance

A live read-only Proxy Path was exercised from Arcane's LAN endpoint to its explicit loopback upstream:

`http://192.168.2.181:3552/`

to:

`http://127.0.0.1:3552/`

The story returned overall `pass`.

Passed stages:

- public DNS;
- public route;
- public TCP;
- public HTTP;
- local listener/container proxy context;
- upstream DNS;
- upstream route;
- upstream TCP;
- upstream HTTP.

The native upstream request and the same upstream tested with the public Host identity both returned HTTP 200, so HostSleuth reported the upstream HTTP behavior as consistent across native and public Host variants.

This exercised the real live M14 engine without Cloudflare or an external proxy dependency.

## Admin Console v2 live acceptance

Live served JavaScript/CSS and headless-browser DOM execution confirmed:

- **Proxy Path** navigation and view;
- visual request-path flow;
- Host/SNI identity comparison section;
- bounded **Copy evidence summary** control;
- global Quick Target console;
- density control;
- Admin Console desktop navigation rail.

## Disaster recovery

`xXDasGoGXx/OMV-Docker-Rebuild` PR #8 updated the recovery image pin to:

`mjmalleo/hostsleuth:0.8.0`

while preserving the existing HostSleuth recovery security/runtime posture.

Recovery PR #8 merged after live acceptance at:

`290e7e6a6781ad29cbc7c8296d00877fb8489fd0`

## Final alignment

Stable/public, live Arcane production, and disaster recovery are aligned on v0.8.0.

M14 Reverse Proxy / Upstream Story + Admin Console v2 is published, verified, and live.

The next approved feature milestone is **M15 — Deployment / Permissions Story**.

# HostSleuth v0.6.0 — Production / Recovery Alignment

Status: complete on 2026-09-17.

## Published release

Stable `v0.6.0` was published from exact source:

`fcb08be51ae3da8cd20dc3929cf9d736b15f170c`

Release workflow:

`35284864117` — success

Verified public Docker OCI index:

`sha256:6b9f90209f477ba8213d9c4bf7db7996d6c52d2caae2cfce405822df8bc8ef2a`

## Live Arcane rollout

The existing Arcane-managed OMV project was updated in place from:

`mjmalleo/hostsleuth:0.5.0`

to:

`mjmalleo/hostsleuth:0.6.0`

No other Compose setting was changed.

Live acceptance confirmed:

- `GET /api/about` reports `v0.6.0`;
- `GET /api/snapshot` reports schema 4 / Docker mode;
- Docker inventory remains readable;
- retained event state survived the redeploy with 100 events available, including history from well before the upgrade;
- the Web UI contains the M12 Expectations view and `/api/contract` client path;
- Action Web/API remains loopback-only and returns HTTP 403 from LAN access;
- a live M12 contract for `192.168.2.181:8787` with `tls=forbidden` and `container=hostsleuth` returned overall `pass`;
- that contract proved DNS, TCP, expected plaintext TLS behavior, and the running HostSleuth container.

## Startup listener churn observation

The first post-redeploy capture briefly recorded the HostSleuth TCP/8787 listener as disappeared while the endpoint itself was already reachable.

The next scheduled snapshot recorded:

- listener appeared again at `192.168.2.181:8787`;
- process ownership = live `hostsleuth` process;
- the host socket table independently showed `192.168.2.181:8787` listening.

This was bounded redeploy/startup churn, not a persistent reachability failure.

## Disaster-recovery alignment

`xXDasGoGXx/OMV-Docker-Rebuild` PR #6 staged the same image pin without merging ahead of production. After live v0.6.0 acceptance passed, PR #6 merged at:

`f78abbe5742c315d73cf85709ebfeba620311429`

The live Arcane deployment and disaster-recovery Compose definition are therefore aligned on:

`mjmalleo/hostsleuth:0.6.0`

## Final boundary

Stable/public/live/recovery are aligned on v0.6.0.

M12 Expected Endpoint Contracts is published and live. The product remains local-first, read-only by default, and bounded; Docker mode still does not expose native systemd Safe Actions.

No new feature milestone is implied by this rollout closeout.

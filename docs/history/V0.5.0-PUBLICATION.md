# HostSleuth v0.5.0 — Publication and Verification

Status: published and independently verified on 2026-09-17. Production/recovery rollout remains a separate step.

## Source

Stable `v0.5.0` was published from exact source commit:

`04a53f8f0f3f48f7118a9ee9a688820cc000a340`

This source includes the Redacted Evidence Bundle merged through PR #40.

Release workflow:

- branch: `release/v0.5.0`
- workflow run: `35281793336`
- result: success

The release workflow completed tests, native amd64/arm64 binary builds, GitHub release/tag publication, Docker Hub authentication, and stable multi-platform image publication.

## GitHub release

Release/tag:

`v0.5.0`

Published assets:

- `hostsleuth-linux-amd64` — `sha256:2626df7ae8d7ad7926b68c2be1b22beb6b489c3128e4cc15bf0e5bcde9f1960d`
- `hostsleuth-linux-arm64` — `sha256:3088738b5aad3dba76def866ec4f5aeac3eef7754a8a6107bfc175841cb1dc77`
- `SHA256SUMS` — `sha256:e758930e892e5f6d64952b11cbb9664b37ad05569aa8398d9c0799b9b8f8653e`

## Independent native verification

The published amd64 binary and `SHA256SUMS` were downloaded independently on the OMV host.

Verification passed:

```text
hostsleuth-linux-amd64: OK
```

The published binary reports:

```text
v0.5.0 (04a53f8f0f3f)
```

The new Evidence Bundle CLI is present; invoking `hostsleuth evidence` without a subcommand correctly reports that `preview` or `export` is required.

## Docker publication

Published stable tags:

- `mjmalleo/hostsleuth:0.5.0`
- `mjmalleo/hostsleuth:latest`

Anonymous Docker Registry verification confirmed both tags resolve to the same OCI image index:

`sha256:a17325980d5e9ec9760f9003aa8a9490962bb06ffbbd5a393a0ad31a218e480d`

Platform manifests:

- linux/amd64 — `sha256:ed8fb39f4aa656e95a5cd5cfecb454c5861a77d610bbc8c3b1d27b61292dc8a7`
- linux/arm64 — `sha256:8ec337d3f0a42576e86fe08cdb69f3725414f7e1255c4b7f759841a522943d72`

The index body SHA-256 also matched the registry content digest for both `0.5.0` and `latest`.

## Release boundary

Publication is complete and verified. This does **not** by itself change the live Arcane deployment or the `OMV-Docker-Rebuild` recovery pin.

At publication closeout:

- public stable: `v0.5.0` / `mjmalleo/hostsleuth:0.5.0`;
- Docker `latest`: v0.5.0 index above;
- live Arcane deployment: still `mjmalleo/hostsleuth:0.4.0`;
- disaster-recovery definition: still `mjmalleo/hostsleuth:0.4.0`.

Production/recovery alignment to v0.5.0 must be performed and accepted separately.

# HostSleuth v0.9.0 — Production / Recovery Alignment

Status: production and disaster recovery aligned on 2026-09-17 after public v0.9.0 publication and live acceptance.

## Release identity

Stable/public release:

`v0.9.0`

Exact published source:

`b518ed901e2d3f4e95a9bb74ade37d7b3a156540`

Release workflow:

`35310749834` — success

Public Docker image:

`mjmalleo/hostsleuth:0.9.0`

Verified OCI index:

`sha256:c99f417419b864756d246232602a8f0fb31067fc11324430a59baec65d62dc5b`

Publication record:

`docs/history/V0.9.0-PUBLICATION.md`

## Live Arcane rollout

The existing Arcane-managed `hostsleuth` project was updated only from:

`mjmalleo/hostsleuth:0.8.0`

to:

`mjmalleo/hostsleuth:0.9.0`

No volume, host-network, PID/UTS, command, security, certificate, DNS, proxy, mail-service, firewall, or unrelated project settings were changed.

v0.8.0 remained the known-good rollback image during acceptance.

## Live acceptance

The live endpoint at `http://192.168.2.181:8787` passed the following checks.

### Identity / retained state

- `GET /api/about` returned `v0.9.0`.
- `GET /api/snapshot` returned schema 4 in Docker mode.
- Docker inventory reported the running `hostsleuth` container image as `mjmalleo/hostsleuth:0.9.0`.
- `GET /api/events` returned all 100 retained events, including history from before the upgrade.

### M15 Deployment / Permissions

A real request to `/api/permissions-story` was accepted by the live v0.9.0 server.

In Docker mode, native systemd service identity remained intentionally unavailable and the story failed closed at `service-identity` rather than crossing the container boundary.

The served Admin Console bundle contains the Permissions interface.

### M16 STARTTLS / Mail Service Story

A real live SMTP STARTTLS check against `192.168.1.190:25` passed:

- TCP;
- greeting;
- STARTTLS capability;
- protocol upgrade;
- TLS handshake;
- certificate validity;
- certificate trust.

Hostname validation failed only because the acceptance target was deliberately the numeric IP rather than a hostname on the certificate. This was expected and demonstrates the stage model remained precise.

The served Admin Console bundle contains the STARTTLS interface.

### M17 Certificate Rollout Verification

A real live reference-vs-endpoint direct-TLS comparison against `example.com:443` returned:

- overall status: pass;
- one endpoint total;
- one MATCH;
- zero mismatch/unknown;
- certificate validity healthy;
- hostname healthy;
- trust healthy.

The served Admin Console bundle contains the Cert Rollout interface.

### M18 Safe Actions boundary

LAN access to:

`GET /api/actions`

still returned HTTP 403 because Action Web/API operations remain loopback-only.

A loopback-source capability query returned both fixed actions:

- `service.restart`;
- `service.reload`.

Both were:

- `enabled=false`;
- `available=false`.

This confirms the supported Docker deployment did not gain native systemd mutation capability from M18.

## Disaster recovery alignment

`xXDasGoGXx/OMV-Docker-Rebuild` recovery PR #10 was staged before production acceptance and merged only after live v0.9.0 passed.

Recovery merge:

`813b7a8b76bfd987e97c534c76faf879c510bdec`

Recovery now pins:

`mjmalleo/hostsleuth:0.9.0`

`CURRENT-SETUP.md` records the same live acceptance and deployment boundary.

## Final state

Stable/public/live/recovery are aligned on:

`v0.9.0` / `mjmalleo/hostsleuth:0.9.0`

The published Docker `latest` tag also resolves to the verified v0.9.0 OCI index.

The approved source roadmap through M18 is complete, published, live, and recovery-aligned.

No further feature milestone or additional Safe Action family is approved automatically.

# HostSleuth v1.0.0 — Production / Recovery Alignment

Status: production and disaster recovery aligned on 2026-09-18 after public v1.0.0 publication and live acceptance.

## Release identity

Stable/public release:

`v1.0.0`

Exact published source:

`27971e67ad7ea875d83f5925a83c1991b0eb2b0f`

Release workflow:

`35358798799` — success

Public Docker image:

`mjmalleo/hostsleuth:1.0.0`

Verified OCI index:

`sha256:aff5482b4c81bd11261afae3d1793dd0a180b9dbf0ddcbc54714c33b99074917`

Publication record:

`docs/history/V1.0.0-PUBLICATION.md`

## Live Arcane rollout

The existing Arcane-managed `hostsleuth` project was updated only from:

`mjmalleo/hostsleuth:0.9.0`

to:

`mjmalleo/hostsleuth:1.0.0`

No volume, host-network, PID/UTS, command, security, state, certificate, DNS, mail-service, firewall, or unrelated project settings were changed.

v0.9.0 remained the known-good rollback image throughout acceptance.

## Live acceptance

The live endpoint at `http://192.168.2.181:8787` passed the v1.0.0 acceptance checklist.

### Identity / retained state

- `GET /api/about` returned `v1.0.0`.
- `GET /api/snapshot` returned schema 4 in Docker mode.
- Docker inventory reported the running `hostsleuth` container image as `mjmalleo/hostsleuth:1.0.0`.
- `GET /api/events` returned all 100 retained events.
- Retained events include records from before the v1.0.0 redeploy, proving state continuity.
- Loopback-source `GET /api/actions/audit` remained readable and returned an empty audit list; no retained Safe Action audit records were present.

### v1 browser / HTTP hardening

Live responses include:

- restrictive Content-Security-Policy;
- Permissions-Policy disabling camera, microphone, and geolocation;
- Referrer-Policy: no-referrer;
- X-Content-Type-Options: nosniff;
- X-Frame-Options: DENY.

Observed served sizes remained within the accepted budgets:

- HTML: 10,624 bytes <= 64 KiB;
- CSS: 63,753 bytes <= 128 KiB;
- JavaScript: 161,171 bytes <= 256 KiB.

A live headless-Chrome check loaded the actual Admin Console and confirmed all expected views:

- Overview;
- Diagnose;
- Changes;
- Host;
- Reboot;
- Workbench;
- Expectations;
- DNS Detective;
- Proxy Path;
- Permissions;
- STARTTLS;
- Cert Rollout;
- Actions.

A `#cert-rollout` deep link selected the correct tab/panel with:

- `aria-selected=true`;
- `tabindex=0`;
- `aria-hidden=false`.

Chrome was launched with a narrow `390x844` requested window. Its effective page width was 485 CSS pixels / 500 inner pixels, below the responsive desktop layout range, and document scroll width equaled client width. No horizontal document overflow was present.

### M15 Deployment / Permissions

A real live request for:

`ssh.service` -> `/etc/ssh`

returned:

- status: `unknown`;
- first problem: `service-identity`;
- conclusion: native service identity is unavailable in Docker deployment mode.

This is the expected fail-closed Docker boundary.

### M16 STARTTLS / Mail Service Story

A real SMTP STARTTLS check against:

`192.168.1.190:25`

passed:

- TCP;
- SMTP greeting;
- STARTTLS capability;
- STARTTLS upgrade;
- TLS negotiation;
- certificate validity;
- certificate trust.

TLS negotiated as TLS 1.3 with `TLS_AES_128_GCM_SHA256`.

Hostname validation failed only because the acceptance target was deliberately the numeric IP rather than a certificate hostname. The served certificate remained valid and trusted.

### M17 Certificate Rollout Verification

A real reference-vs-endpoint direct-TLS comparison against:

`example.com:443`

returned:

- overall status: pass;
- one endpoint total;
- one MATCH;
- zero mismatch;
- zero unknown;
- certificate validity healthy;
- hostname healthy;
- trust healthy.

### M18 Safe Actions boundary

LAN access to:

`GET /api/actions`

still returned HTTP 403 because Action Web/API operations remain loopback-only.

A loopback-source capability query returned both fixed native actions:

- `service.restart`;
- `service.reload`.

Both were:

- `enabled=false`;
- `available=false`.

This confirms the supported Docker deployment gained no native systemd mutation capability in v1.0.0.

### Evidence Bundle boundary

The Evidence Bundle implementation did not change between v0.9.0 and v1.0.0.

The live server exposes no Web/API evidence-export route:

- `/api/evidence` -> HTTP 404;
- `/api/evidence/export` -> HTTP 404.

The existing bounded CLI-only evidence-export security model therefore remains intact.

## Disaster recovery alignment

`xXDasGoGXx/OMV-Docker-Rebuild` recovery PR #11 was staged before production acceptance and merged only after live v1.0.0 passed.

Recovery merge:

`44d6a30e580e2b333841f14eade2e10cca4b5ba5`

Recovery now pins:

`mjmalleo/hostsleuth:1.0.0`

`CURRENT-SETUP.md` records the same live acceptance and deployment boundary.

## Final state

Stable/public/live/recovery are aligned on:

`v1.0.0` / `mjmalleo/hostsleuth:1.0.0`

The published Docker `latest` tag resolves to the same verified v1.0.0 OCI index:

`sha256:aff5482b4c81bd11261afae3d1793dd0a180b9dbf0ddcbc54714c33b99074917`

The v1.0 readiness/hardening cycle and release/production/recovery sequence are complete.

No additional feature milestone or Safe Action family is automatically approved.

# HostSleuth v0.3.0 Publication

Date: 2026-09-16

## Release source

Stable `v0.3.0` was published from exact accepted main commit:

`6e6b45ca5e4a4c54897ad69a3b20a377e68fccb1`

This release contains the accepted M5 configuration-fingerprinting and M6 Certificate Story / TLS Detective source state.

## Controlled release path

The existing `.github/workflows/release.yml` workflow was used through branch `release/v0.3.0`.

GitHub Actions Release run:

`35138865223`

Result: `success`.

The workflow re-ran the release tests, built native binaries, generated checksums, created the GitHub release/tag, and published the stable multi-platform Docker image.

## GitHub release

Release: `v0.3.0`

Target commit:

`6e6b45ca5e4a4c54897ad69a3b20a377e68fccb1`

Published assets:

- `hostsleuth-linux-amd64`
- `hostsleuth-linux-arm64`
- `SHA256SUMS`

Downloaded copies of both native binaries passed `sha256sum -c SHA256SUMS`.

One bounded real-consumer check executed the downloaded amd64 binary on the actual OMV Debian host from `/tmp`; `hostsleuth version` returned:

`v0.3.0 (6e6b45ca5e4a)`

No production HostSleuth process or configuration was changed for this acceptance.

## Docker publication

Published stable tags:

- `mjmalleo/hostsleuth:0.3.0`
- `mjmalleo/hostsleuth:latest`

Anonymous Docker Registry v2 verification confirmed both tags resolve to the same OCI index:

`sha256:127b388fbf794841b22d06b281fe89dc1500188fdb4215eec023b392aa98c05d`

Verified platform manifests:

- `linux/amd64`
- `linux/arm64`

The registry verification used anonymous pull-scope tokens and did not rely on Docker Hub credentials.

## Live OMV boundary

Publication did **not** migrate the live OMV/Arcane deployment.

The known-good live deployment remains intentionally pinned to:

`mjmalleo/hostsleuth:0.1.0`

`xXDasGoGXx/OMV-Docker-Rebuild` remains pinned to the same `0.1.0` recovery state.

Release publication, production migration, and recovery-pin changes remain separate owner-controlled decisions.

## Next milestone boundary

M7 — Service Story is next in the approved roadmap but is not started by this publication closeout. Begin M7 only after explicit owner direction.

# HostSleuth v0.4.0 Publication

Date: 2026-09-17

## Accepted source

Stable `v0.4.0` was published from exact accepted main commit:

`6566c505b32cc47d96384152a988736173d3f7cd`

That commit is the M11 merge and contains the completed M7 Service Story, M8 Incident Lens, M9 Workbench, M10 Reboot Story, and M11 Optional Safe Actions source that accumulated after v0.3.0.

Post-merge main CI run #216 completed successfully before release publication.

## Release workflow

Release branch:

`release/v0.4.0`

Release workflow run:

- run number: `5`
- run ID: `35193948370`
- conclusion: `success`

The workflow re-ran Go tests, built linux/amd64 and linux/arm64 native binaries, generated `SHA256SUMS`, created the GitHub tag/release, authenticated to Docker Hub, and published stable multi-platform Docker tags.

## GitHub release

Tag/release:

`v0.4.0`

Target commit:

`6566c505b32cc47d96384152a988736173d3f7cd`

Published assets:

- `hostsleuth-linux-amd64`
  - `sha256:0ea9c20ade7a96fe208aef4b29416f6ebc831dc971e87d2f6ff07d8f93b03233`
- `hostsleuth-linux-arm64`
  - `sha256:017144dfd5cddf5fb5a351318079d094687650d1ba6bc2759a9eceeae1c82d83`
- `SHA256SUMS`
  - `sha256:b7c5dc0ab4c62287e28e0c5ce051d598a3bf202f6c062e605c72c382eeb01f58`

## Independent native consumer verification

The published amd64 binary and `SHA256SUMS` were downloaded on OMV into an isolated `/tmp` directory.

Verification passed:

- `sha256sum -c SHA256SUMS --ignore-missing` -> `hostsleuth-linux-amd64: OK`
- binary version -> `v0.4.0 (6566c505b32c)`
- `hostsleuth action list` -> fixed `service.restart` capability present but `enabled: false` / `available: false` by default.

This proves the published binary is the intended v0.4.0 source and preserves M11's disabled-by-default boundary.

## Docker publication

Published tags:

- `mjmalleo/hostsleuth:0.4.0`
- `mjmalleo/hostsleuth:latest`

Independent Docker Hub metadata verification confirmed both tags resolve to the same multi-platform OCI index:

`sha256:03b5824fddc50a707e5486033afed3f01d0be76e9adef64292d7a72743578bf0`

Platform manifests:

- linux/amd64 — `sha256:f001ab1537184b4841688b5838dff5c7fca95ce7c2dfcf6eb578ec1a441b7299`
- linux/arm64 — `sha256:4e70aa4a4439801d5e14d89d1193ab87befdc96a56fad107a725fee412dd0320`

## Deployment boundary

Publication did **not** change the live OMV HostSleuth container.

At publication closeout, live production still reports:

- application version `v0.3.0`;
- snapshot schema 3;
- deployment mode `docker`;
- image `mjmalleo/hostsleuth:0.3.0`.

The disaster-recovery `main` branch also remains pinned to `0.3.0`.

With explicit owner approval, `xXDasGoGXx/OMV-Docker-Rebuild` PR #4 stages the future recovery image `mjmalleo/hostsleuth:0.4.0`, but that PR remains intentionally unmerged until the live Arcane-managed project is upgraded and verified.

The remaining operational step requires an authenticated Arcane session/API. Arcane health is reachable, but its project API rejects unauthenticated access. HomeCommander correctly blocks raw Docker/sudo control, and its old native `hostsleuth` managed-deployment record is not the active Arcane/Docker deployment. No bypass was attempted.

## Product boundary after publication

`v0.4.0` is now the current stable public release and contains M7–M11.

The Redacted Evidence Bundle is still **NOT STARTED**. Publication and deployment cleanup do not authorize that later milestone automatically.
